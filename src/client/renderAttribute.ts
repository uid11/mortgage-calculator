import type {AttributeSlot} from '../types';

/**
 * Renders attribute slot.
 * This client function should not use scope variables (except other client functions).
 */
export const renderAttribute: AttributeSlot['render'] = function () {};
