import type {ChildSlot} from '../types';

/**
 * Renders child slot.
 * This client function should not use scope variables (except other client functions).
 */
export const renderChild: ChildSlot['render'] = function () {};
